Clone this repo to .vim and symlink .vim/vimrc to ~/.vimrc 
ln -s ~/.vim/vimrc ~/.vimrc

To get all the git submodules in /bundle run
git submodule init
git submodule update
rm -rf ~/.vim/bundle/snipmate.vim/snippets        (I keep snippets in .vim/snippets)
